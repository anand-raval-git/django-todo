apiVersion: v1
kind: Service
metadata:
  name: django-app-service
  namespace: app-deployment
spec:
  type: Nodeport
  selector:
    app: django-todo
  ports:
    - port: 80
      target port: 8000
      node port: 3009

